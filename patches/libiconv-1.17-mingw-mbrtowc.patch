--- lib/loop_wchar.h.orig	2022-05-15 16:00:00.000000000 +0000
+++ lib/loop_wchar.h	2024-01-15 12:00:00.000000000 +0000
@@ -36,7 +36,11 @@
 # include <wchar.h>
 # define BUF_SIZE 64  /* assume MB_LEN_MAX <= 64 */
   /* Some systems, like BeOS, have multibyte encodings but lack mbstate_t.  */
+#if !defined __MINGW32__
   extern size_t mbrtowc ();
+#else
+  /* MinGW-w64 provides mbrtowc in headers, don't redeclare it */
+#endif
 # ifdef mbstate_t
 #  define mbrtowc(pwc, s, n, ps) (mbrtowc)(pwc, s, n, 0)
 #  define mbsinit(ps) 1
